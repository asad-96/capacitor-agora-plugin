// https://google-fonts.nuxtjs.org/options
// https://google-fonts.nuxtjs.org/downloading

export const googleFontsConfig = {
  // prefetch: true,
  // preconnect: true,
  // preload: true,
  download: true,
  // base64: false,
  useStylesheet: true, // should be true for AMP sites
  display: 'swap', // 'auto' | 'block' | 'swap' | 'fallback' | 'optional'
  families: {
    Quicksand: true
  }
}
